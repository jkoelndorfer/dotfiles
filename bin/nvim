#!/bin/zsh

# Wrapper script for neovim.

# pyenv seems to make jedi unhappy. It will refuse to run
# because it does not recognize the Python as safe.
# See https://github.com/davidhalter/jedi/issues/1250.

pathmunge_reorder "$HOME/.pyenv/shims" after
export PATH
exec /usr/bin/nvim "$@"
