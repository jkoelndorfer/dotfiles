#!/bin/bash

"$DOTFILE_DIR/hdmisw/hdmisw.py" "$HDMI_SWITCH_ACCELERATED_HDMI_INPUT"
xrandr --output "$GAMING_MONITOR_DISPLAYNAME" --off

# It seems that compton with trayone's patches does not like it when an
# output is turned off. The output on the remaining screen becomes garbage.
#
# To workaround this, restart it.
"$DOTFILE_DIR/i3/i3helpers"
