#!/bin/zsh

display_profile=$("$DOTFILE_DIR/bin/gui/display-profile")

if [[ "$display_profile" == 'UHD' ]]; then
    addl_args=(-o 'font.size=24.0')
else
    addl_args=(-o 'font.size=12.0')
fi

exec alacritty "${addl_args[@]}" "$@"
