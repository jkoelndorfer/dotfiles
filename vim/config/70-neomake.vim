call neomake#configure#automake('rw')

" virtualtext shows an error immediately next to the code.
" This tends to make the screen a little busy for my liking,
" so turn it off.
let g:neomake_virtualtext_current_error = 0
