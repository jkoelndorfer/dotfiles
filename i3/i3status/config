general {
    interval = 5
}

load {
    format = "%1min"
}

wireless _first_ {
    format_up = "%bitrate @ %ip"
    format_down = "down"
}

volume master {
    device = "pulse"
    format = "🔊 %volume"
    format_muted = "🔇%volume"
}

time {
    format = "%a %Y-%m-%d %H:%M"
}

order += "volume master"
order += "wireless _first_"
order += "load"
order += "time"
